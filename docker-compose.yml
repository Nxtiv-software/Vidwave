version: '3.8'

services:
  vidwave:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vidwave-landing
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production

